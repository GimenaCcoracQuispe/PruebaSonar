spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false  # opcional si usas Flyway
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      enabled: false
    consumer:
      enabled: false
